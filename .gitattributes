# Normalize line endings: text files are stored and checked out as LF regardless
# of a contributor's core.autocrlf setting, so formatting stays consistent across
# Windows, macOS, and Linux. text=auto lets git auto-detect and leave binary
# files untouched.
* text=auto eol=lf
